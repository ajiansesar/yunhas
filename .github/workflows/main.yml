name: vpsrun
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: thismyrunner
    steps:
      - run: sudo apt install neofetch -y
      - run: curl -sSf https://sshx.io/get | sh -s run
